name: Compiler

on: [push]

jobs:
  build_osx:
    runs-on: macOS-10.14
    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: brew install squashfs
    - name: run build
      run: make build
